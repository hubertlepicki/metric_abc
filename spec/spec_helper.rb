require File.expand_path(File.join(File.dirname(__FILE__), "..", "lib", "metric_abc"))

RSpec.configure do |config|
  config.mock_with :rspec

  #config.before(:each) do
  #end

  #config.after(:each) do
  #end
end

